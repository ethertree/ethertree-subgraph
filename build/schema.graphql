type Tree @entity {
  id: ID!
  startDate: BigInt!
  endDate: BigInt! # uint256
  bounty: BigInt! # address
  fee: BigInt!
  paymentSize: BigInt!
  paymentFrequency: BigInt!
  lapseLimit: BigInt!
  minWaterers: BigInt!
}
